# のまぼど LINEミニアプリ プロジェクト記憶ファイル

最終更新: 2026-08-21

---

## プロジェクト概要

ボードゲームカフェ「のまぼど」向けのLINEミニアプリ。
顧客がLINEアプリ内でチェックイン・チェックアウトを行い、滞在時間に応じた料金を自動計算する。
スタッフ向けの管理画面では顧客リスト・在室者確認・料金設定変更ができる。

---

## 技術スタック

| 用途 | 技術 |
|---|---|
| フレームワーク | Next.js 14 (App Router, TypeScript) |
| スタイリング | Tailwind CSS |
| データベース | Neon (PostgreSQL) via Vercel |
| ORM | Prisma v5 |
| LINE連携 | LIFF SDK (@line/liff) |
| 決済 | Stripe v14（未接続・準備済み） |
| ホスティング | Vercel |

---

## 重要URL・アカウント情報

### アプリURL
- **本番ユーザー向け（LINE内）**: `https://miniapp.line.me/2010087859-VQRBMyHs`
- **本番Webアクセス**: `https://nomabodo-liff.vercel.app`
- **管理画面**: `https://nomabodo-liff.vercel.app/admin-login`

### 管理画面ログイン
- **パスワード（ADMIN_ACCESS_TOKEN）**: `nomabodo-admin`
  ※ Vercelの環境変数に設定済み

### GitHub
- **リポジトリ**: `https://github.com/gifty-eiki/nomabodo-liff`
- **ユーザー名**: `gifty-eiki`
- **ブランチ**: `main`

### Vercel
- **プロジェクト**: `https://vercel.com/gifty-eikis-projects/nomabodo-liff`
- **デプロイ方法**: `git push` で自動デプロイ

### Neon（データベース）
- **コンソール**: `https://console.neon.tech`
- **プロジェクト名**: `neon-coffee-lever`
- **接続文字列（pooler）**: `postgresql://neondb_owner:npg_Dz8RU7WVTPne@ep-weathered-scene-aq3bholr-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require`

### LINE Developers
- **コンソール**: `https://developers.line.biz/console`
- **プロバイダー**: のまぼど
- **チャネル名**: のまぼど（LINEミニアプリ）
- **本番用LIFF ID**: `2010087859-VQRBMyHs`
- **本番用エンドポイントURL**: `https://nomabodo-liff.vercel.app`

---

## ローカル開発環境

### 必要なもの
- Node.js
- PowerShell

### 起動方法
```powershell
cd "E:\Claudeプロジェクト\project\のまぼど\LINEミニアプリ\nomabodo-liff"
npx prisma generate --schema prisma/schema.prisma   # 初回/スキーマ変更時のみ
npm run dev
```
→ `http://localhost:3000` で開く

### 開発モード
- `.env.local` に `NEXT_PUBLIC_LIFF_ID=dev-mode` が設定済み。
- `dev-access-token` でLINE認証をバイパス（`lib/line.ts` の DEV_TOKEN。dev-user は `dev-user-001`）。
  - API直叩き例: `curl -H "Authorization: Bearer dev-access-token" http://localhost:3000/api/status`
- 管理画面: `http://localhost:3000/admin-login` → パスワードは **`nomabodo-admin`**
  （※ 画面の「開発用トークンをセット」ボタンが入れる `dev-access-token` では入れない。要注意）

### ローカルDB＝本番Neonに直結
- ローカルからも**本番Neon(PostgreSQL)に直接接続**する構成（`.env.local` に `POSTGRES_PRISMA_URL` / `POSTGRES_URL_NON_POOLING`）。
  - 理由: `schema.prisma` はPostgreSQL版で固定する必要があり、旧SQLite版(`schema.sqlite.backup`)は新フィールド欠落で使えないため。
- ⚠️ **ローカルでチェックイン等のテストをすると本番DBに書き込まれる。** テストデータは後で掃除すること（→「今後の課題」参照）。

### ローカルのStripe
- `.env.local` の `STRIPE_SECRET_KEY` 等は**コメントアウトして未設定**にしている。
  - ダミーキーを入れると退室API(`/api/checkout`)が `StripeAuthenticationError` で500になるため。
  - 未設定だと checkout は決済をスキップし、支払いを `succeeded` として記録（本番Vercelと同じ挙動）。

---

## Gitプッシュ方法

通常は以下でOK：
```powershell
cd "E:\Claudeプロジェクト\project\のまぼど\LINEミニアプリ\nomabodo-liff"
git add -A
git commit -m "変更内容"
git push
```

認証が求められた場合（トークン切れ）:
1. `https://github.com/settings/tokens` でClassicトークンを新規作成（repoにチェック）
2. 以下で実行:
```powershell
git push https://gifty-eiki:取得したトークン@github.com/gifty-eiki/nomabodo-liff.git main
```

---

## 本番DBへのマイグレーション手順

⚠️ **重要**: `schema.prisma` は **常にPostgreSQL版のまま**維持すること。
Vercelのビルド時にこのファイルでPrismaクライアントを生成するため、SQLiteに変えるとビルドエラーになる。

スキーマ変更時は以下をPowerShellで実行：

```powershell
cd "E:\Claudeプロジェクト\project\のまぼど\LINEミニアプリ\nomabodo-liff"

# Neon接続情報をセット
$env:POSTGRES_PRISMA_URL="postgresql://neondb_owner:npg_Dz8RU7WVTPne@ep-weathered-scene-aq3bholr-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
$env:POSTGRES_URL_NON_POOLING="postgresql://neondb_owner:npg_Dz8RU7WVTPne@ep-weathered-scene-aq3bholr.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require"

# スキーマ適用
npx prisma db push
```

---

## プロジェクト構成（主要ファイル）

```
nomabodo-liff/
├── app/
│   ├── page.tsx                    # ユーザー画面（チェックイン/アウト・アンケート制御）
│   ├── admin/
│   │   ├── layout.tsx              # 管理者認証ガード
│   │   ├── page.tsx                # ダッシュボード（在室者・本日来訪者・売上）
│   │   ├── customers/page.tsx      # 顧客一覧（最終利用日・プレイヤーネーム表示）
│   │   ├── customers/[id]/page.tsx # 顧客詳細
│   │   └── pricing/page.tsx        # 料金設定
│   ├── admin-login/page.tsx        # 管理者ログイン
│   └── api/
│       ├── checkin/route.ts        # チェックインAPI
│       ├── checkout/route.ts       # チェックアウトAPI（プラン制・学割・土日祝を計算）
│       ├── status/route.ts         # 状態確認API（プラン・学割・土日祝加算・在室数を返す）
│       ├── survey/route.ts         # 初回アンケート保存API
│       ├── player-name/route.ts    # プレイヤーネーム変更API
│       ├── upsert-profile/route.ts # LINEプロフィール保存
│       └── admin/
│           ├── login/route.ts          # 管理者ログイン
│           ├── pricing/route.ts        # 料金プラン(PricePlan)の取得/更新
│           ├── force-checkout/route.ts # 強制退室
│           └── customers/[id]/route.ts # 顧客更新（学割ON/OFF PATCH）
├── components/
│   ├── customer/
│   │   ├── CheckInButton.tsx       # 入室ボタン（button.png画像を表示）
│   │   ├── CheckOutCard.tsx        # 退室カード＋退室完了画面（プラン名/学割/土日祝バッジ）
│   │   ├── SurveyModal.tsx         # 初回アンケートモーダル
│   │   └── EditPlayerNameModal.tsx # プレイヤーネーム編集モーダル
│   ├── admin/
│   │   ├── PricingEditor.tsx       # 料金プラン編集（上限時間・金額）
│   │   └── StudentToggle.tsx       # 学割ON/OFFトグル
│   └── liff/
│       └── LiffProvider.tsx        # LIFF初期化
├── lib/
│   ├── auth.ts                     # 管理者JWT認証
│   ├── billing.ts                  # 料金計算（pickPlan / getPlanCost / applyStudentDiscount）
│   ├── holidays.ts                 # 土日祝判定（サーバー専用・@holiday-jp/holiday_jp）
│   ├── db.ts                       # Prismaクライアント
│   ├── line.ts                     # LINE token検証
│   └── stripe.ts                   # Stripeクライアント
├── prisma/
│   ├── schema.prisma               # ⚠️ 常にPostgreSQL版を維持（Vercelビルドに使用）
│   ├── schema.production.prisma    # schema.prismaと同内容のバックアップ
│   ├── schema.sqlite.backup        # 旧SQLite版（未使用・参照しない）
│   └── seed.ts                     # 初期データ（プラン3件・開発管理者）
└── public/
    ├── logo.jpg                    # のまぼどロゴ（カラフルなイラスト）
    ├── background.png              # 背景画像（ボードゲームカフェ内装）
    └── button.png                  # 入室ボタン画像（木製円形デザイン）
```

---

## Vercel環境変数一覧

| 変数名 | 用途 |
|---|---|
| `NEXT_PUBLIC_LIFF_ID` | `2010087859-VQRBMyHs` |
| `NEXT_PUBLIC_APP_URL` | `https://nomabodo-liff.vercel.app` |
| `ADMIN_COOKIE_SECRET` | 管理者セッションのJWT署名キー |
| `ADMIN_ACCESS_TOKEN` | `nomabodo-admin`（管理画面ログインパスワード） |
| `DATABASE_URL` | Neon接続URL（Vercel自動設定） |
| `POSTGRES_PRISMA_URL` | Neon pooler URL（Vercel自動設定） |
| `POSTGRES_URL_NON_POOLING` | Neon direct URL（Vercel自動設定） |
| `STRIPE_SECRET_KEY` | **未設定**（Stripe導入時に追加） |
| `STRIPE_WEBHOOK_SECRET` | **未設定**（Stripe導入時に追加） |

---

## DBスキーマ（Profileモデル）

```prisma
model Profile {
  id               String   @id @default(cuid())
  lineUserId       String   @unique
  displayName      String?  // LINE表示名
  pictureUrl       String?
  isAdmin          Boolean  @default(false)
  isStudent        Boolean  @default(false) // 学割フラグ（ONで料金半額）
  stripeCustomerId String?
  realName         String?  // 本名（初回アンケート）
  playerName       String?  // プレイヤーネーム（変更可）
  gender           String?  // 男性/女性/その他
  ageGroup         String?  // 10代〜60代以上
  region           String?  // 延岡市/宮崎市/日向市/門川町/上記以外の宮崎県内/宮崎県外
  surveyCompleted  Boolean  @default(false)
  createdAt        DateTime @default(now())
  ...
}
```

---

## 料金設定

管理画面 → 料金設定 から変更可能。

### 料金体系：プラン制（2026-07-28に従量制から変更）
滞在時間で**自動判定**する段階制（方式B）。上限時間が短いプランから順に判定し、収まる最初のプランを適用。
- **2時間**（maxMinutes=120）→ ¥2,000
- **4時間**（maxMinutes=240）→ ¥3,000
- **フリー**（maxMinutes=null＝上限なし）→ ¥3,500
- **会員**（サブスク有効）→ ¥0

計算例:
- 90分滞在 → 2時間プラン **¥2,000**
- 121分滞在 → 4時間プラン **¥3,000**
- 300分滞在 → フリー **¥3,500**

DBモデル: `PricePlan`（label / maxMinutes(null=フリー) / amountYen / sortOrder / isActive）。
- ⚠️ 旧 `PricingConfig`（30分従量）は**未使用**（テーブルは残置・削除しない）。
- billing: `lib/billing.ts` の `pickPlan` / `getPlanCost`。
- プラン料金は本番Neonに投入済み（2h=2000 / 4h=3000 / フリー=3500・仮の金額）。

### 学割・土日祝の加算（2026-07-28）
料金の最終計算は次の順序:
1. プラン料金 = 滞在時間から自動判定（`pickPlan`）。会員は¥0。
2. 学割ONならプラン料金を半額（`Math.floor(base/2)`）
3. **土日祝なら一律 +¥500**（入室日基準・JST・日本の祝日カレンダー。会員¥0は対象外。学割の半額対象外＝そのまま上乗せ）

例: 学生が土曜にフリー利用 → 3500÷2 + 500 = **¥2,250**（検証済み）
例: 学生が平日に4時間 → 3000÷2 = **¥1,500**
- 実装: `lib/holidays.ts`（`getWeekendHolidaySurcharge` / `isWeekendOrHoliday`、`@holiday-jp/holiday_jp` 使用）
- ⚠️ `lib/holidays.ts` は祝日データ(大)を含むため**クライアントからimport禁止**。加算額はstatus APIからpropsで渡す。

---

## 最大収容人数

`app/page.tsx` 内の定数で設定:
```typescript
const MAX_CAPACITY = 20
```
変更する場合はこの値を書き換えてプッシュする。

---

## 実装済み機能

### ユーザー向け
- [x] LINEログイン（LIFF）
- [x] **初回アンケート**（本名・プレイヤーネーム・性別・年代・地域 → 2回目以降はスキップ）
- [x] **プレイヤーネーム変更**（ヘッダーの名前タップで編集モーダル表示）
- [x] チェックイン（button.png画像ボタン）
- [x] リアルタイム滞在時間・料金表示（「分」を小さく表示）
- [x] **学割表示**（学割ONの人は目安料金・最終料金が半額で表示され「🎓 学割適用（半額）」バッジ）
- [x] チェックアウト → 完了画面で滞在時間・料金を表示
- [x] 現在の在室人数表示（/20名）
- [x] デザイン（background.png背景・logo.jpg・button.png）

### 管理者向け
- [x] パスワードログイン
- [x] ダッシュボード（現在の在室者・本日の来訪者・売上・会員数）
- [x] 顧客一覧（登録順/五十音順・来店回数・**最終利用日**・プレイヤーネーム表示・**本名・性別・年代・地域（アンケート情報）表示**）
- [x] 顧客詳細ページ
- [x] 料金設定の変更（**プラン制**：2時間/4時間/フリーの金額・上限時間を編集可）
- [x] 強制退室機能（プラン制・学割・土日祝を反映）
- [x] **学割設定**（顧客詳細ページのトグルでON/OFF・顧客一覧に学割バッジ）

---

## 追加メニュー（フード・ドリンク）注文機能（2026-08-21）

入室後の画面から追加メニューを注文でき、退室時の合計に加算される。

### 仕様
- ユーザー画面（`CheckOutCard`）に「🍽️ メニューを注文」ボタン → `MenuOrderModal`。
  - ポテト/お菓子/たこ焼き/カレー = ＋/−で個数指定（kind="count"）
  - ドリンクバー = ON/OFFトグル（kind="toggle"・1回のみ加算）
  - 「ご注文はレジまでお越しください」の文言を表示
  - 「決定」で `POST /api/order` に**現在の数量で上書き保存**（何度でも修正可）
- 料金合計 = プラン料金（学割・土日祝込み）＋ **注文合計**。
  - ⚠️ 注文合計は**学割・土日祝の対象外＝実額**で加算（会員¥0でも注文分は課金）。
- 管理画面 `/admin/menu`（ナビ「メニュー設定」）で品名・価格・表示ON/OFFを編集。

### DBモデル（追加）
- `MenuItem`（label / priceYen / kind("count"|"toggle") / sortOrder / isActive）
- `OrderItem`（visitSessionId / menuItemId / quantity・`@@unique([visitSessionId, menuItemId])`）
  - VisitSession に `orderItems OrderItem[]` リレーション追加。
- 初期メニュー（暫定価格）: ポテト¥300 / お菓子¥300 / たこ焼き¥400 / カレー¥500 / ドリンクバー¥400。

### 関連ファイル
- `lib/menu.ts`（型 MenuItemLite / OrderLine・`calcOrderTotal`）
- `app/api/order/route.ts`（注文の上書き保存）
- `app/api/status/route.ts`（menu・order・orderTotal を返す）
- `app/api/checkout/route.ts`（amountYen に orderTotal を加算）
- `components/customer/MenuOrderModal.tsx` / `CheckOutCard.tsx`
- `app/admin/menu/page.tsx` / `components/admin/MenuEditor.tsx` / `app/api/admin/menu/route.ts`

### 学割・会員と注文の関係（重要・確認済み）
- **学割（半額）はプラン利用料のみ**。フード・ドリンク（注文合計）は割引対象外＝実額。
- **会員（サブスク有効）は利用料¥0**（`getPlanCost`が0を返す・土日祝加算も対象外）。フード・ドリンクのみ加算。
- ＝ 合計 = 利用料(会員0/学割半額) + 土日祝(会員0) + 注文合計(実額)。

### カフェルール表示（2026-08-21）
- トップ画面フッターの「現在のご利用人数」表示は**非表示化**し、代わりに
  **「📖 のまぼどカフェ ルール」ボタン**を設置 → `RulesModal` をポップアップ表示。
- ルール文言は仮（ボードゲームカフェ想定）。`components/customer/RulesModal.tsx` の `RULES` 配列を編集すれば差し替え可能。
- 在室人数の取得ロジック(`currentOccupancy`)自体は残置（表示のみ停止）。

### 本番DB反映（2026-08-21 実施済み）
- [x] 本番Neonにテーブル追加: `prisma db push` 完了（MenuItem/OrderItem）
- [x] 初期メニュー5品を投入: `prisma db seed` 完了（ポテト/お菓子/たこ焼き/カレー/ドリンクバー）
- [x] ローカル型チェック(`npx tsc --noEmit`)通過
- [ ] **Vercelへデプロイ**（`git push`）— コミット/プッシュはユーザー指示待ち

---

## 未実装・今後の課題

- [ ] **Stripe決済の接続**（退室後に実際の支払い処理）
  - Stripe Dashboardで商品・価格を作成
  - `STRIPE_SECRET_KEY`・`STRIPE_WEBHOOK_SECRET`をVercelに追加
  - Webhook URL: `https://nomabodo-liff.vercel.app/api/stripe/webhook`
- [ ] **月額サブスクリプション機能**（`/subscription`ページは実装済み）
- [ ] **LINEリッチメニュー設定**（LINEの友達がかんたんにアクセスできるように）
- [ ] **LINEミニアプリ審査申請**（公式リリースに必要）
- [x] **顧客一覧・詳細ページにアンケート情報表示**（管理者がrealName・gender・ageGroup・regionを確認できるように → 顧客一覧に追加済み）
- [ ] **独自ドメイン設定**（必要であれば）

---

## 費用・インフラ状況

| サービス | プラン | 費用 |
|---|---|---|
| Vercel | Hobby（無料） | ¥0/月 |
| Neon | Free（0.5GB） | ¥0/月 |
| LINE Mini App | 無料 | ¥0/月 |
| Stripe | 決済手数料のみ | 決済額の3.6% |

→ **Stripe導入まで完全無料で運用可能**

---

## トラブルシューティング

### ユーザーがログインできない
→ LINEアプリ内でアクセスしているか確認。ブラウザからは使えない。

### 「Invalid token」エラー
→ LINEのアクセストークンの期限切れ。LINEアプリを再起動して再アクセス。

### 管理画面にログインできない
→ パスワードは `nomabodo-admin`。大文字小文字に注意。

### デプロイが失敗する（TypeErrorなど）
→ Vercelダッシュボード → Deployments → ログを確認。
→ よくある原因: `schema.prisma` がSQLiteになっている。常にPostgreSQL版を維持すること。

### Vercelビルドで「Property 'xxx' does not exist」エラー
→ `prisma/schema.prisma` に新しいフィールドが含まれているか確認。
→ SQLiteバックアップから restore した場合は新フィールドが消えるので注意。

### 背景画像・ロゴが表示されない
→ `next/image` の `fill` プロパティは使わず、CSS `background-image` か通常の `<img>` タグを使う。
→ `public/` フォルダのファイル名は英数字のみ推奨（日本語ファイル名は問題を起こすことがある）。

---

*このファイルはプロジェクトの記憶・引継書です。変更があれば随時更新してください。*
